streamlit>=1.36
pyspark>=4,<5  # Spark 4.x requires Java 17+ (set JAVA_HOME accordingly)
plotly>=5.22
pandas>=2.2,<3  # pandas 3.x defaults string columns to Arrow-backed dtype --
                # avoided as defense in depth alongside the pyarrow pin below
requests>=2.31  # dataset download (pipeline/dataset_manager.py)
xgboost>=2.1    # GPU-accelerated gradient boosting (SparkXGBRegressor, device="cuda")
scikit-learn>=1.3  # required by xgboost.spark

# pyarrow 25.0.0 (the version pip otherwise resolves, transitively via pyspark)
# segfaults in libarrow.so's array-building code -- confirmed via the WSL2
# kernel log, reproducibly, every time Streamlit renders an st.dataframe()
# (Streamlit serializes tables to Arrow internally regardless of any Spark or
# pandas setting). Pinned to a version well below the break; satisfies both
# pyspark's (>=15.0.0) and streamlit's (>=7.0) minimums.
pyarrow==19.0.1
